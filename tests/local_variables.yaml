cases:
  - name: basic local variables
    status: 0
    stdout: "3\n"

templates:
- test.js: |
    function main() {
      const a = 1;
      const b = 2;
      const c = a + b;
      console.log(c);
    }
