test_path: tests
parallelize_runners: true

runners:
  - name: jsc
    setup:
      - yarn init --yes
      - yarn add /root/project
    run: |
      yarn ts-node ./node_modules/jsc/src/main.ts test.js
      node ./bin/index.js
