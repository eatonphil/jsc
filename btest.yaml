test_path: tests
parallelize_runners: true

runners:
  - name: jsc
    setup:
      - yarn init --yes
      - yarn add ts-node typescript rimraf node-gyp @types/node
    run: |
      yarn ts-node /root/project/src/main.ts test.js
      node ./bin/index.js
