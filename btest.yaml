test_path: tests
parallelize_runners: true

runners:
  - name: jsc
    setup:
      - yarn init --yes
      - yarn add ts-node typescript rimraf node-gyp @types/node
    run: |
      . /etc/profile
      yarn ts-node $ROOT_DIRECTORY/src/main.ts test.js
      node ./bin/index.js
