test_path: tests
parallelize_runners: true

runners:
  - name: jsc
    setup:
      - yarn init --yes
      - yarn add ts-node typescript rimraf node-gyp @types/node
      - yarn ts-node $CIRCLE_WORKING_DIRECTORY/src/main.ts test.js
    run: node ./bin
