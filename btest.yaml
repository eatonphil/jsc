test_path: tests
parallelize_runners: true

runners:
  - name: jsc
    setup:
      - yarn init --yes
      - yarn add /root/project
    run: |
      node ./node_modules/jsc/build/main test.js &> /dev/null
      node ./bin/index.js
